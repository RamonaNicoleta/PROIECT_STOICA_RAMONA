@page
@model Proiect_Stoica_Ramona.Pages.Cars.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Car</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Car.Id" />
            <div class="form-group">
                <label asp-for="Car.Name" class="control-label"></label>
                <input asp-for="Car.Name" class="form-control" />
                <span asp-validation-for="Car.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Car.AvailabeDate" class="control-label"></label>
                <input asp-for="Car.AvailabeDate" class="form-control" />
                <span asp-validation-for="Car.AvailabeDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Car.Model" class="control-label"></label>
                <input asp-for="Car.Model" class="form-control" />
                <span asp-validation-for="Car.Model" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Car.Price" class="control-label"></label>
                <input asp-for="Car.Price" class="form-control" />
                <span asp-validation-for="Car.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Car.LocationID" class="control-label">Location Name</label>
                <select asp-for="Car.LocationID" class="form-control" asp-items="ViewBag.LocationID">
                    <option value="">Select a Location</option>
                </select>
                <span asp-validation-for="Car.LocationID" class="text-danger"></span>
            </div>
            <!-- Display EngineTypes as checkboxes -->
            <div class="form-group">
                <label>Engine Types</label>
                @foreach (var engineType in Model.EngineTypes)
                {

                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" asp-for="@engineType.Assigned" />
                        <label class="form-check-label" for="@("EngineTypes_" + engineType.Id)">
                            @engineType.Name
                        </label>
                        <input type="hidden" asp-for="@engineType.Id" />
                    </div>
                }
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
